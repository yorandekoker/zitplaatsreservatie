{
  "version": 2,
  "builds": [
    {
      "src": "server/app.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/public/(.*)",
      "dest": "/server/public/$1"
    },
    {
      "src": "/(.*)",
      "dest": "server/app.ts"
    }
  ]
}